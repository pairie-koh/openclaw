// link-understanding defaults helpers and runtime behavior.
/** Reused constant for DEFAULT LINK TIMEOUT SECONDS behavior in src/link-understanding. */
export const DEFAULT_LINK_TIMEOUT_SECONDS = 30;
/** Reused constant for DEFAULT MAX LINKS behavior in src/link-understanding. */
export const DEFAULT_MAX_LINKS = 3;
