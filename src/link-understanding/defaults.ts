// Defaults for link-understanding fetches and per-message link extraction.
/** Default timeout for guarded link fetches and adapter execution. */
export const DEFAULT_LINK_TIMEOUT_SECONDS = 30;
/** Default cap for unique links extracted from one message. */
export const DEFAULT_MAX_LINKS = 3;
