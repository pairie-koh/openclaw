/** Shared system prompt build option types. */
export type PromptMode = "full" | "minimal" | "none";
/** Shared type for Silent Reply Prompt Mode in src/agents. */
export type SilentReplyPromptMode = "generic" | "none";
