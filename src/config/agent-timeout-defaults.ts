// config agent timeout defaults helpers and runtime behavior.
/** Reused constant for DEFAULT LLM IDLE TIMEOUT SECONDS behavior in src/config. */
export const DEFAULT_LLM_IDLE_TIMEOUT_SECONDS = 120;
