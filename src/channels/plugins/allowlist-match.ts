// Channel plugin facade for allowlist match types.
/** Re-exported API for src/channels/plugins, starting with Allowlist Match. */
export type { AllowlistMatch, AllowlistMatchSource } from "../allowlist-match.js";
/** Re-exported API for src/channels/plugins, starting with format Allowlist Match Meta. */
export { formatAllowlistMatchMeta } from "../allowlist-match.js";
