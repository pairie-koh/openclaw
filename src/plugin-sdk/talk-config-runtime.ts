/** Runtime SDK barrel for active talk provider config resolution. */
export { resolveActiveTalkProviderConfig } from "../config/talk.js";
/** Re-exported API for src/plugin-sdk, starting with Open Claw Config. */
export type { OpenClawConfig } from "../config/types.js";
