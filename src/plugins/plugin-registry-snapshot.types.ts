// Shared types for plugins plugin registry snapshot types behavior.
/** Shared type for Plugin Registry Snapshot Source in src/plugins. */
export type PluginRegistrySnapshotSource = "provided" | "persisted" | "derived";
