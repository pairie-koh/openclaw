// Prompt image ordering mode shared by prompt composition and media offload code.
/** Controls whether prompt images remain inline or move to offloaded media references. */
export type PromptImageOrderEntry = "inline" | "offloaded";
