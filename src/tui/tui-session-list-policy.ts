// tui tui session list policy helpers and runtime behavior.
/** Reused constant for TUI RECENT SESSIONS ACTIVE MINUTES behavior in src/tui. */
export const TUI_RECENT_SESSIONS_ACTIVE_MINUTES = 7 * 24 * 60;
/** Reused constant for TUI SESSION PICKER LIMIT behavior in src/tui. */
export const TUI_SESSION_PICKER_LIMIT = 50;
/** Reused constant for TUI SESSION LOOKUP LIMIT behavior in src/tui. */
export const TUI_SESSION_LOOKUP_LIMIT = 5;
