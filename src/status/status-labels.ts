// Small reusable labels for status text surfaces.
/** Formats the current fast-mode state for compact status output. */
export const formatFastModeLabel = (enabled: boolean): string => `Fast: ${enabled ? "on" : "off"}`;
