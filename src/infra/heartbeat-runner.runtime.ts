// Lazy runtime boundary for heartbeat reply execution.
/** Reply runner used by heartbeat wakes after the scheduler loads the runtime chunk. */
export { getReplyFromConfig } from "../auto-reply/reply.js";
