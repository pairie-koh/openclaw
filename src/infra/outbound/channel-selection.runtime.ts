// Lazy runtime boundary for outbound channel selection.
/** Channel selection resolver loaded by outbound runtime call sites. */
export { resolveMessageChannelSelection } from "./channel-selection.js";
