// Runtime helpers for group policy resolution.
import { getChannelPlugin, normalizeChannelId } from "../../channels/plugins/index.js";

/** Re-exported API for src/auto-reply/reply, starting with get Channel Plugin. */
export { getChannelPlugin, normalizeChannelId };
