// Shared types for gateway hooks types behavior.
import type { ChannelId } from "../channels/plugins/types.public.js";

/** Shared type for Hook Message Channel in src/gateway. */
export type HookMessageChannel = ChannelId;
