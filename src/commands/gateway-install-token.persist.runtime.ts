/** Runtime config write re-exports for gateway install token persistence. */
export { readConfigFileSnapshotForWrite } from "../config/io.js";
/** Re-exported API for src/commands, starting with replace Config File. */
export { replaceConfigFile } from "../config/mutate.js";
