// extensions/diffs runtime api helpers and runtime behavior.
/** Re-exported diffs plugin public API, starting with resolve Request Client Ip. */
export { resolveRequestClientIp } from "openclaw/plugin-sdk/webhook-ingress";
